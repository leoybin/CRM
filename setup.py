from setuptools import setup
setup(name='crmCustomer',
      version='1.0.1',
      description='test for zip',
      author='yb',
      author_email='623374149@qq.com',
      packages=['crmCustomer'],
      # packages=find_packages()可替换上个packages,可自动寻找到所有文件，当promax下有多文件夹时
      # 可用
      )

